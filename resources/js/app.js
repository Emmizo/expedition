import './bootstrap';

// Import Bootstrap JS
import * as bootstrap from 'bootstrap';
window.bootstrap = bootstrap; // Optional: make bootstrap globally available

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();
